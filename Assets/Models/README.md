# Bob ONNX models

Copy the best exported policy here after training:

```bash
cp results/bob-v4/Bob/*.onnx Assets/Models/Bob.onnx
```

Then: **Bob → Demo → Enable Inference Only**

> Use **bob-v4** (after [ml-training-recommendations.md](../../docs/design/ml-training-recommendations.md) Tier 1). bob-v3 checkpoints had 0% makes — not demo-ready.
