[pytest]
testpaths = tests
pythonpath = .
markers =
    requires_mlagents: needs mlagents-learn on PATH (CI/Docker; skipped on Apple Silicon local venv)
    rag: needs chromadb (install python/requirements-rag.txt)
